# Використовуємо образ, який у вас гарантовано завантажується
FROM bitnami/spark:3

# Перемикаємось на користувача з правами root для встановлення пакетів
USER root

# Встановлюємо Python-бібліотеку для роботи з MySQL
RUN pip install --no-cache-dir mysql-connector-python

# Повертаємось на стандартного користувача образу
USER 1001